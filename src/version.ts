/** Tool version stamped into emitted artifacts' provenance. Keep in sync with package.json. */
export const TOOL_VERSION = '0.1.0';

/** Salesforce API version the plugin queries with. */
export const API_VERSION = '62.0';
